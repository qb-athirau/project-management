import styled from 'styled-components';

export const StyledArrow = styled.div`
         background: transparent;
         cursor: pointer;
         border:none;
         :focus{
           outline:none;
         }
         }
       `;
